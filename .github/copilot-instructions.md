# Copilot Instructions

## Project Guidelines
- User prefers class names that use "Policy" suffix for static classes that hold immutable, per-operation configuration rules (e.g., StorageSourcePolicy).
- When modifying existing code, always preserve the user's original comments exactly as written. Only remove or update a comment if it has become factually obsolete due to the change being made.